<nav class="navbar bg-dark navbar-dark navbar-expand-lg fixed-top pb-2">
	<a href="{{ path('home') }}"><img src="{{ asset('build/images/secretlab.png') }}" alt="logo" class="navbar-logo mx-4 logo"></a>
	<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="navbar-collapse collapse justify-content-end" id="navbarSupportedContent">
		<ul class="navbar-nav ml-auto text-center">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">Accueil</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('seat_index') }}">Liste</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('seat_new') }}">Ajouter</a>
			</li>
		</ul>
	</div>
	{% if is_granted('IS_AUTHENTICATED_FULLY') %}
		Bienvenue
																	!
		<a class="bg-danger text-white border border-white text-decoration-none mx-2 rounded-2 foot-link" href="{{ path('app_logout') }}">Deconnexion</a>
	{% else %}
		<a class="text-white border border-white text-decoration-none mx-2 rounded-2 foot-link" href="{{ path('app_login') }}">Se connecter</a>
	{% endif %}
</nav>
