<nav class="navbar bg-dark navbar-dark navbar-expand-lg fixed-top pb-2">
	<div class="container-fluid">
		<a href="{{ path('home') }}"><img src="{{ asset('build/images/secretlab.png') }}" alt="logo" class="navbar-logo mx-4 logo"></a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="navbar-collapse collapse justify-content-end" id="navbarSupportedContent">
			<div class="navbar-nav">
				<a class="nav-link" href="{{ path('home') }}">Accueil</a>
				<a class="nav-link" href="{{ path('admin_list') }}">Liste</a>
                <a class="nav-link" href="{{ path('admin_add') }}">Ajouter</a>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					Bienvenue
					!
					<a class="nav-link btn btn-secondary" href="{{ path('app_logout') }}">Deconnexion</a>
				{% else %}
					<a class="nav-link btn btn-secondary" href="{{ path('app_login') }}">Se connecter</a>
				{% endif %}
			</div>
		</div>
	</div>
</nav>
