<nav class="navbar bg-dark navbar-expand-lg fixed-top pb-2">
	<a href="{{ path('home') }}"><img src="{{ asset('build/images/logo.png') }}" alt="logo" class="navbar-logo mx-4 logo"></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="navbar-collapse collapse justify-content-end" id="navbarSupportedContent">
		<ul class="navbar-nav ml-auto text-center">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">Accueil</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('seat') }}">Siège</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('techno') }}">Technologie</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('contact') }}">Contact</a>
			</li>
		</ul>
	</div>
</nav>
